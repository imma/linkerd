#!/usr/bin/env bash

function server {
  local shome="$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"
  source "$shome/script/profile"

  exec linkerd "$shome/vendor/linkerd/config/linkerd.yaml"
}

server "$@"
